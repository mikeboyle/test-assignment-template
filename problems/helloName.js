/**
 * A function that takes a name and returns the string "Hello, {name}"
 * @param {string} name Any name
 * @returns {string} "Hello, {name}"
 */
const helloName = (name) => `Hello, ${name}`;

module.exports = {
  helloName,
};
